/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry
} from 'react-native';

import DefaultPropsPropTypes from './app';

AppRegistry.registerComponent('DefaultPropsPropTypes', () => DefaultPropsPropTypes);
